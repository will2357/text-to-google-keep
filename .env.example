# Django
DJANGO_SECRET_KEY=change-me-in-production
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,testserver

# Database: PostgreSQL (default) or set DJANGO_USE_SQLITE=true for local SQLite
# DJANGO_USE_SQLITE=true
DB_NAME=ttgk_dev
DB_USER=ttgk
DB_PASSWORD=ttgk_local
DB_HOST=localhost
DB_PORT=5432
# Test DB name (Postgres only; Django creates/drops it for manage.py test)
DB_TEST_NAME=ttgk_test

# Google OAuth client JSON path (CLI + Django web)
# GOOGLE_KEEP_CLIENT_SECRETS=/path/to/client_secret.json
